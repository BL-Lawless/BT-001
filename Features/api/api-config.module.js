(() => {
  "use strict";

  window.API_CONFIG_MODULE = {
    version: "FEATURES_API_CONFIG_V1",
    sources: {
      restService: "window.restService",
      websocketService: "window.websocketService"
    }
  };
})();

